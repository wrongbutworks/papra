{
  "name": "@papra/app",
  "version": "26.6.2",
  "private": true,
  "description": "Papra application package - document management system (CalVer versioned).",
  "repository": {
    "type": "git",
    "url": "https://github.com/papra-hq/papra",
    "directory": "packages/app"
  },
  "scripts": {
    "build:docker:root": "docker build --no-cache -t papra-local-root -f ./Dockerfile ../..",
    "build:docker:root:amd64": "docker buildx build --platform linux/amd64 -t papra-local-root-amd64 -f ./Dockerfile --load ../..",
    "build:docker:root:arm64": "docker buildx build --platform linux/arm64 -t papra-local-root-arm64 -f ./Dockerfile --load ../..",
    "build:docker:rootless": "docker build --no-cache -t papra-local-rootless -f ./Dockerfile.rootless ../.."
  }
}
